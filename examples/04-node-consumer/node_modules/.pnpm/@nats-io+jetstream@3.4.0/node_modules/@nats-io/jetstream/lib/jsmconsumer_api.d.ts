import { BaseApiClientImpl, type JetStreamApiRequestOptions } from "./jsbaseclient_api";
import type { Nanos, NatsConnection } from "@nats-io/nats-core/internal";
import type { ConsumerConfig, ConsumerCreateOptions, ConsumerInfo, ConsumerResetResponse, ConsumerUpdateConfig } from "./jsapi_types";
import type { ConsumerAPI, JetStreamOptions, Lister } from "./types";
export declare class ConsumerAPIImpl extends BaseApiClientImpl implements ConsumerAPI {
    constructor(nc: NatsConnection, opts?: JetStreamOptions);
    addUpdate(stream: string, cfg: ConsumerConfig, opts: Partial<JetStreamApiRequestOptions>, delta?: Partial<ConsumerConfig>): Promise<ConsumerInfo>;
    private minConsumerApi;
    private requiredApiOpts;
    add(stream: string, cfg: ConsumerConfig, opts?: ConsumerCreateOptions): Promise<ConsumerInfo>;
    update(stream: string, durable: string, cfg: ConsumerUpdateConfig): Promise<ConsumerInfo>;
    info(stream: string, name: string): Promise<ConsumerInfo>;
    delete(stream: string, name: string): Promise<boolean>;
    list(stream: string): Lister<ConsumerInfo>;
    pause(stream: string, name: string, until: Date): Promise<{
        paused: boolean;
        pause_until: string;
        pause_remaining: Nanos;
    }>;
    resume(stream: string, name: string): Promise<{
        paused: boolean;
        pause_until?: string;
    }>;
    unpin(stream: string, name: string, group: string): Promise<void>;
    reset(stream: string, name: string, seq?: number): Promise<ConsumerResetResponse>;
}
